nodeId: node-1
logger:
  level: debug
nodeServer:
  port: :40041
server:
  port: :50051
  nodes:
    - localhost:40042
    - localhost:40043
repository:
  type: cassandra
cassandra:
  hosts:
    - localhost
  keyspace: cloud
  templatesRoot: repository/cassandra/queries/templates
client:
  serverAddress: localhost:50051
  clientId: "Smith"
